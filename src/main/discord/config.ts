/**
 * Discord Rich Presence configuration.
 *
 * Paste your Discord Application ID here to enable Rich Presence. Create an app
 * at https://discord.com/developers/applications, copy its **Application ID**
 * (General Information), and under **Rich Presence → Art Assets** upload an
 * image named exactly `logo` (used as the large icon).
 *
 * Leave empty to disable Rich Presence entirely (no connection is attempted).
 */
export const DISCORD_CLIENT_ID: string = ''

/** The art-asset key uploaded in the Discord app (Rich Presence → Art Assets). */
export const DISCORD_LARGE_IMAGE = 'logo'
